name: Update Articles Daily

on:
  schedule:
    - cron: '0 7 * * *'  # 매일 오전 7시 (UTC) / 팔로알토(PDT) 새벽 0시
  workflow_dispatch:  # 수동 실행 가능

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11' # 파이썬 버전 명시
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Check API Key
        env:
          THE_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          if [ -z "$THE_KEY" ]; then
            echo "⛔ CRITICAL ERROR: GEMINI_API_KEY not found in Secrets!"
            exit 1
          else
            echo "✅ API Key found. Length: ${#THE_KEY}"
          fi
      
      - name: Run scraper
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }} # GitHub Secrets에서 API 키 가져오기
        run: python -u scraper.py
      
      - name: Commit and push if changed
        run: |
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'actions-bot@github.com'
          git add articles.json logs.json  # logs.json도 같이 커밋하면 디버깅에 좋음
          
          # 변경 사항이 있을 때만 커밋
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "📰 자동 업데이트: 기사 목록 갱신"
            git push
          fi

